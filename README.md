cd python
pip install -r requirements.txt
python def4ult_admin_finder.py


cd nodejs
npm install
node def4ult_admin_finder.js <target-url>

cd go
go run def4ult_admin_finder.go <target-url>

#ZIP extract
#ZIP download
cd ~/Downloads
unzip Def4ult-Admin-Finder.zip -d ~/Def4ult-Admin-Finder
cd ~/Def4ult-Admin-Finder

cd python
pip3 install -r requirements.txt

python3 def4ult_admin_finder.py

#Node.js dependencies install

cd ../nodejs
npm install
node def4ult_admin_finder.js https://example.com
cd ../go

go run def4ult_admin_finder.go https://example.com




unzip ~/Downloads/Def4ult-Admin-Finder.zip -d ~/Def4ult-Admin-Finder && cd ~/Def4ult-Admin-Finder






### Def4ult Admin Finder

Multi-language Admin Finder Tool (Python / Node.js / Go)  
Fast scanner, multithreaded, colorful CLI.

### Usage

### Python
```bash


#one-click comman
git clone https://github.com/xdefult-coder/Admin-Finder.git ~/Def4ult-Admin-Finder && cd ~/Def4ult-Admin-Finder






